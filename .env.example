# API Configuration
# Set to "/" for root paths (default), "/api" for /api prefix, or any custom prefix
# Examples:
#   API_BASE_PATH=/      → routes at root level (e.g., /auth/login, /health)
#   API_BASE_PATH=/api   → routes with prefix (e.g., /api/auth/login, /api/health)
#   API_BASE_PATH=/v1    → routes with prefix (e.g., /v1/auth/login, /v1/health)
API_BASE_PATH=/

# Database Configuration
DATABASE_URL=postgresql://sexto_andar_user:sexto_andar_pass@postgres:5432/sexto_andar_db
# OR use individual components:
# DB_HOST=postgres
# DB_PORT=5432
# DB_NAME=sexto_andar_db
# DB_USER=sexto_andar_user
# DB_PASSWORD=sexto_andar_pass

# Database Connection Settings
DB_READY_MAX_ATTEMPTS=30
DB_READY_DELAY_SECONDS=1.0
SQL_DEBUG=false

# JWT Configuration
# IMPORTANT: Change JWT_SECRET_KEY in production!
JWT_SECRET_KEY=your-super-secret-key-change-in-production
JWT_ALGORITHM=HS256
JWT_ACCESS_TOKEN_EXPIRE_MINUTES=30

# Inter-Service Communication
# URL da API de imóveis (comunicação interna via Docker network)
PROPERTIES_API_URL=http://sexto-andar-properties-api:8000

# Secret compartilhado para autenticação entre serviços
# IMPORTANTE: Deve ser EXATAMENTE o mesmo valor nos dois serviços (auth e properties)
# Em produção, gerar novo secret com: python3 -c "import secrets; print(secrets.token_urlsafe(32))"
INTERNAL_API_SECRET=change-this-to-secure-random-secret-in-production

# ===== Cookie Configuration =====
# For cross-origin development, set COOKIE_SAMESITE to "none" and COOKIE_SECURE to "true".
# You may also need to set COOKIE_DOMAIN to a common parent domain (e.g., "localhost").
# Note: SameSite="none" requires Secure="true".
# COOKIE_DOMAIN=localhost
# COOKIE_SAMESITE=lax
# COOKIE_SECURE=false
